{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "app", "camera-core", "core", "kit", "math", "physics2d", "physics3d", "renderer-core", "schema", "vite",
        "deps", "ci", "release", "docs"
      ]
    ]
  }
}
