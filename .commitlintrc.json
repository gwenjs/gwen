{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "app", "core", "kit", "math", "physics2d", "physics3d", "schema", "vite",
        "deps", "ci", "release", "docs"
      ]
    ]
  }
}
