[package]
name = "gwen-wasm-utils"
version.workspace = true
edition.workspace = true
description = "Shared utilities for GWEN WASM plugins — buffer I/O and ring-buffer events"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
paste = "1"

